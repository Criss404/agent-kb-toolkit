# 知识库结构(agent 写入前必读)

> 创建日期:YYYY-MM-DD
> 本文件是这个知识库的**结构声明**:目录路由、编号规则、写作偏好都以此为准。
> agent 执行任何知识库写入前,必须先读本文件并按它路由;本文件不存在或读不到时,向用户报告,禁止凭记忆猜结构。

## 目录路由表

| 目录 | 放什么 | 编号前缀 | MOC(索引) |
|---|---|---|---|
| `<域名1>/` | (一句话:这个域收什么主题) | XX-NN | [[XX-00-域索引]] |

<!-- 策展区清单(机器可读,查重脚本按此扫描;每行一个目录,勿改格式) -->
```curated-dirs
<域名1>
```

## 非策展区(不是笔记,不参与查重,禁止写入笔记)

- (临时文件、外来资料等目录,按需列出)

## 编号与引用规则

- 系列内 `前缀-NN` 递增,写入前先列目录确认下一个可用编号,禁止覆盖已有文件
- 编号一经分配保持稳定,系列出现空缺不重排
- 跨目录关联靠正文 [[wikilink]] 互链;每篇文末有"相关笔记"

## 路由判断规则

1. 先按主题对号入座(上表)
2. 归属拿不准或沾边不足时,向用户提问并等待选择,候选清单含"新建目录"方案
3. 新领域经用户同意后,新建"域文件夹 + 前缀 + XX-00 MOC",并回写本文件的路由表和策展区清单

## 写作偏好(本库所有笔记遵循)

- 链接风格:wikilink + 嵌入(Obsidian 约定;非 Obsidian 阅读器下嵌入显示为原始语法,数据不受影响)
- (术语、来源标注等偏好按需补充)
